@Mock

Creates a fake dependency.

@InjectMocks

Creates the real class under test.

when(...).thenReturn(...)

Programs the mock's behavior.

assertThrows(...)

Verifies an exception is thrown.

Arrange
Act
Assert
