## Context vs Dockerfile

These are independent concepts.

Context:

- Defines what Docker can access.

Dockerfile:

- Defines how to build the image.

Think of it as:

Context = Workspace

Dockerfile = Recipe
